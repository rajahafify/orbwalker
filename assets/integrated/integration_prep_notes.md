# Runtime Integration Prep Notes

No generated asset in this workspace may be copied into `resources/art/first_pass/`, `resources/visual/`, scenes, scripts, or project settings until the approval gate passes.

After approval, integration should happen in a separate focused change that updates runtime asset maps or resource paths, then validates with Godot MCP where runtime behavior is affected.
